//对于创建目录，或者创建文件，都是需要一个绝对路径的，否则就是创建失败。楼主上边的demo路径不是通用的，各位看官要写自已的路径，以下就是我的路径。本人亲测，一切OK.
var fs = require('fs');
console.log('创建目录');
fs.mkdir('D:\\a\\b',function(err){
    if(err){
      return console.error(err);
    }
    console.info('创建目录成功...');
});
